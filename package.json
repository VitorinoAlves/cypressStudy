{
  "name": "poc_cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cy:open:qa": "cypress open --env environment=qa",
    "cy:run:qa": "npx cypress run --env environment=qa"
  },
  "author": "c.alves",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.9.0",
    "cypress-dotenv": "^2.0.0",
    "cypress-mochawesome-reporter": "^3.4.0",
    "dotenv": "^16.0.3",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0"
  },
  "dependencies": {
    "config": "^3.3.9"
  }
}
